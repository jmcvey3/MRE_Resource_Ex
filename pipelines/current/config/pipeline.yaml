classname: pipelines.current.pipeline.Current

triggers:
  - .*b5m.*\.nc

retriever:
  path: pipelines/current/config/retriever.yaml

dataset:
  path: pipelines/current/config/dataset.yaml

quality:
  path: shared/quality.yaml
storage:
  path: shared/storage.yaml
